Data layout:
- raw: untouched downloads (SST-2 CSVs)
- interim: cleaned and normalized data
- processed: tokenized or feature-ready datasets
- external: external resources (WordNet cache, etc.)

Keep large artifacts out of git.
